subsection mesh
  set mesh_path = ../../../mesh/2D/cavity.msh
end

subsection boundary conditions
  set bc_data_path = inputs/cavity/bc.h5

  # bc0 = (6, 0, 1.0, uniform) 
  # bc1 = (6, 1, 0.0, uniform)
  # bc2 = (7, 0, 0.0, uniform)
  # bc3 = (7, 1, 0.0, uniform)
end

subsection finite element
  set degree_vel = 1
  set degree_pre = 1
end

subsection fluid
  set mu = 0.01
end

set output_dir = test
set output_interval = 10
set verbose = false